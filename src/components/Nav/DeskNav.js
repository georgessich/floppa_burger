import classes from '../Header/Header.module.css';
import Nav from './Nav';
const DeskNav = () => {
    return (
        <nav className={classes.DeskNav}>
            <Nav />
        </nav>
    )
}

export default DeskNav;